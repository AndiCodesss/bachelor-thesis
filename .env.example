# Copy this file to `.env` and fill in real values.

# Raw Databento parquet directory
NQ_DATA_PATH="/mnt/c/Users/<YOUR_USER>/Downloads/generator/data/NQ_raw"

# Claude Code CLI binary (optional override, defaults to `claude`)
CLAUDE_CODE_BIN="claude"
